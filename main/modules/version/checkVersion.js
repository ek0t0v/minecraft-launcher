/**
 * Проверяет версию, сравнивает файлы клиента с файлами на сервере.
 *
 * @param version
 *
 * @returns {Promise<*>}
 */
module.exports = async function checkVersion(version) {
    return version;
};